{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
{% include "navbar.html.twig" %}
<div class="container py-4">
    <h2 class="mb-4">Tableau de Bord - Courbe d'évolution</h2>

    <div class="row">
        <div class="col-12 col-lg-10 mx-auto">
            <div class="card shadow rounded-3">
                <div class="card-header bg-white font-weight-bold">
                    Évolution de la mortalité et de la consommation
                </div>
                <div class="card-body">
                    {# Affichage automatique de la courbe Chart.js #}
                    {{ render_chart(chart) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
