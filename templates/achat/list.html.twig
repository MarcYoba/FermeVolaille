{% extends 'base.html.twig' %}

{% block title %}magasin{% endblock %}

{% block body %}
{% include "navbar.html.twig" %}
<section class="vh-100" style="background-color: #eee;">
  <div class="container h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-lg-12 col-xl-11">
        <div class="card text-black" style="border-radius: 25px;">
          <div class="card-body p-md-5">
            <div class="row justify-content-center">

                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th scope="col">Id</th>
                            <th scope="col">Produit</th>
                            <th scope="col">Quantite</th>
                            <th scope="col">Prix</th>
                            <th scope="col">Montant</th>
                            <th scope="col">Operation</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for achat in achats %}
                            <tr>
                                <th scope="row">{{ achat.id }}</th>
                                <td>{{ achat.produit.nom }}</td>
                                <td>{{ achat.quantite }}</td>
                                <td>{{ achat.prix }}</td>
                                <td>{{ achat.montant }}</td>
                                <td>{{ achat.createtAt | date('Y-m-d') }}</td>
                                <td>
                                    <a href="{{ path('app_achat_edit', {'id': achat.id}) }}" class="btn btn-primary">Edit</a>
                                    <a href="{{ path('app_achat_delete', {'id': achat.id}) }}" class="btn btn-danger">Delete</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>

            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
{% endblock %}